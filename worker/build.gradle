// worker/build.gradle
plugins {
    id 'org.springframework.boot'
    id 'io.spring.dependency-management'
}

dependencies {
    implementation project(':core') // The "Inward" rule
    implementation 'org.springframework.boot:spring-boot-starter' // Notice: no "-web" here!
}

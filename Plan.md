1. Functional Requirements ← what the system does
2. Non-Functional Requirements ← how well it does it (consistency, latency, scale bounds)
3. Services & Components ← what logical pieces exist
4. API Contract ← endpoints, request/response shapes
5. Module Boundaries ← internal interfaces, no leaking
6. Low-Level Design ← patterns (Outbox, RLS, Mat. Views)
7. Data Model ← schema, indexes, constraints
8. C4 Architecture Diagram ← everything visualised
